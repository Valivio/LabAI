[project]
name = "labai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "mlx>=0.29.3",
    "mlx-lm>=0.29.1",
]

[project.scripts]
labai = "labai:main"

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"
